# Server
APP_PORT=8080

# Database
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=mampuio_wallet_db
DB_PORT=5432
DB_SSL_MODE=disable # disable or require

DB_URI="postgres://{USER}:{PASSWORD}@{HOST}:{PORT}/{NAME}?sslmode={SSL_MODE}"

# JWT
JWT_SECRET_KEY=""